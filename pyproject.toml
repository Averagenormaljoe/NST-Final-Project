[project]
name = "NST"
version = "0.1.0"
description = "Three NST model project"
name = "StyleMotion"
[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]
requires-python = ">=3.11"
packages = { find = { include = ["shared_utils", "video_utils", "Gatys_model","forward_feed","AdaIN"] } }